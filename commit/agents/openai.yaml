interface:
  display_name: "Commit"
  short_description: "单次 1-6 选项完成提交（可同次选择 push）"
  default_prompt: "Use $commit to review current changes, ask one guided 1-6 choice that combines commit scope and push behavior, then auto-generate a conventional commit message without separate message confirmation."
